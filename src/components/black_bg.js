const { default: styled } = require("styled-components");

const BlackBackground = styled.section`
  background: rgba(42, 75, 89, 0.4);
  width: 100vw;
  height: 100vh;
`;

export default BlackBackground;
